import { Component, Input } from '@angular/core';

@Component({
  selector: 'page-not-found',
  template: `<h1>No such route available</h1>`,
  styles: [`h1 { font-family: Lato; }`]
})
export class PageNotFoundComponent  {
}
